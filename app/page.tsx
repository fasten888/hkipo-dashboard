export default function V2HomePage() {
  return (
    <main className="min-h-screen bg-slate-950 px-8 py-10 text-slate-100">
      <section className="mx-auto max-w-5xl rounded-3xl border border-white/10 bg-white/[0.04] p-8 shadow-2xl">
        <p className="text-xs font-semibold uppercase tracking-[0.24em] text-blue-300">
          Hong Kong IPO OS V2
        </p>
        <h1 className="mt-4 text-4xl font-semibold tracking-tight">
          Personal Investment Command Center
        </h1>
        <p className="mt-3 max-w-2xl text-sm leading-6 text-slate-400">
          V2 app shell placeholder. Current production UI still runs through the
          existing Vite entry while the Next.js architecture is introduced in
          small, safe increments.
        </p>
      </section>
    </main>
  )
}
